---
title: {一句中文判断，写出读者能带走什么}
subtitle: {论文原始标题；必要时先加一句中文解释}
description: {正文写完后再写；用一两句话说清为什么需要这篇、作者多看见了什么、以后怎样判断}
date: [{YYYY-MM-DD Day HH:MM}]
tags: paper
identifier: {YYYYMMDDTHHMMSS}
source: {唯一原始 URL，只写裸 URL，不加描述}
authors: {作者列表}
venue: {发表场所/年份}
---

{所有花括号都是写作提示，成文时必须全部删除。下面三段只是常见起步；可随真实转折合并或拆分。可见标题只能来自论文里的事件、变化或条件。}

# {用具体异常生成标题，例如「为什么手已经按下了电梯」}

{直接进入一件外行马上能想象的小事。自然写清主角是谁、处在什么条件、原来的理解让它做什么、原来期待什么、实际发生什么，不按字段逐项汇报。这个例子会贯穿全文；角色、作用方向、反馈顺序和会改变结论的条件不能删。它应是论文机制的缩小版，不是只求形象的装饰性类比。}

{让读者最自然的理解直接运行。不要给它起名字，也不要预告它会失败。只让论文材料支持的缺口发生：它分不清哪两件事、猜错什么、付出什么代价，或被什么条件挡住。}

# {用作者真正改写的关系或动作生成标题，例如「控制权怎样从态度交给情境」}

{继续使用同一个例子。先写哪条证据让原来的结果站不住，再用普通话说明作者改了哪里、哪个可见结果最先改变。作用第一次可见时才给论文术语；命名后马上重跑原例子，不停下来集中解释定义。}

{把新结果当作下一轮的起点。若仍有缺口，就继续同一条故事。中间确有两个不同的因果转折时，可以增加内容生成的顶层标题；没有就不要硬拆。几个变量若同时作用，用并排案例、状态表或关系图保留同时关系。}

{证据紧跟它支持的判断。分清论文直接测到的、作者给出的解释，以及为了讲清而做的推演。适用范围写在相应结论旁边。}

# {用条件变化生成标题，例如「搬到新楼以后，谁重新接管」}

{从例子起点重新跑一次，把全文的改变串起来。然后用论文里的对照、消融、失败样本或明确讨论的条件变化，换一个关键条件，或换一个表面相似但关键条件不同的情况，看判断在哪里改变。论文没有直接材料时，只做最小机制推演并标明身份。由此写清作者发现的边界。}

{自然落下一条由全文推出的判断规则：以后看到 X，先不急着判断 Y，而先问 Z。没有实际用途，就诚实说明它更像一把识别问题的尺子。}

{只有文字确实难懂时才加入结构图、流程图、曲线、小表格、过程记录或一个最小公式。图放在关系已经用例子讲明白以后；公式下一句立刻翻译成人话。ASCII 图使用 Markdown 围栏代码块，宽度不超过 80 个字符。}
